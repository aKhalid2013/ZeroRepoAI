# .codex

This directory contains the authoritative Codex configuration:
- system_prompt.txt: global rules and guardrails.
- agents/*.txt: role-specific prompts.
- context_map.json: context packs injected per agent.

These files are intentionally visible and must stay in sync with docs/AGENTS.md and docs/WORKFLOW.md.
